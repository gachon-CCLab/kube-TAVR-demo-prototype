apiVersion: apps/v1
kind: Deployment
metadata:
  name: py-server
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nest-server
  template:
    metadata:
      labels:
        app: nest-server
    spec:
      containers:
      - name: nest-server
        image: kakaru0/python-server:latest
        resources:
          limits:
            memory: "64Mi"
            cpu: "0.1"
        ports:
        - containerPort: 5000
